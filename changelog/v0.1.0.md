# v0.1.0

- Refactor: extract tree_connector helper, deduplicate connector logic across content.rs and editor.rs
- UI: Feed names truncate with … in sidebar when too long (non-selected)
- UI: Ghost feed/category origin dims during move (less white)
- UI: Editor panels now use ├─/╰─ tree connectors matching sidebar
- Fix: feeds panel shows category headers as colored non-interactive labels
- Fix: new-category input no longer double-highlights the category below it
- Fix: category move now places item as sibling after cursor (not as child); Left/Right adjusts depth during move
- Fix: feed move cursor can now land on category headers to place feed as first child
- Fix: category headers in feeds panel now show at correct depth (no phantom indent)
- Fix: category no longer highlights on hover during feed/cat move (only preview arrow shows)
- Fix: category deletion confirmation is now a popup instead of a status bar message
- Fix: editor panels use persistent ListState so scroll doesn't stick when navigating up from bottom
- Fix: tree_connector scans forward to correctly show ╰─ for last child with children of its own
- Fix: sidebar scroll position preserved when entering ArticleList and returning
- Fix: settings border now highlights with MAUVE when focused
- Feat: shortcut 'o' opens selected article link in system browser
- Feat: shortcut 'g' jumps to top of feed list
- Feat: shortcut 'C' collapses all categories in sidebar
- UI: feed title scroll starts faster (3-tick pause instead of 8)
- In the article details, show (in colors) how long ago it was shared.
- Have a visual indicator in scrollable spaces that shows how far down you are etc, literally a scrollbar.
- **Pre-fetch on hover** — When navigating in ArticleList, silently fetch article content if stub (without waiting for Enter)
- **ArticleList border** — Removed MAUVE highlight from ArticleList panel border (now always dim SURFACE0)
- **25/25/50 layout in ArticleDetail** — Entering ArticleDetail now keeps the same three-panel layout instead of expanding to full width
- **CategoryPicker black background** — Fixed black background when opening save dialog; underlying three-panel layout now renders beneath the popup
- **SavedCategoryEditor moved from Settings** — Removed from Settings menu; now accessible via `e` shortcut from the Saved tab sidebar (Esc returns to SavedCategoryList)
- **ArticleList scrollbar** — Scrollbar now visible in ArticleDetail state (resolved by layout fix above)
- **Saved sidebar black background** — Added .bg(BASE) to saved sidebar block
- **Star icon replaced** — Removed `★` star icon; circle color now encodes saved state: YELLOW=saved, BLUE=unread, dim=read
- **Article fetch indicator** — Added `article_fetching` bool to App; spinner now appears in article detail title during readability fetches (both prefetch-on-hover and open-article paths)
- **ArticleList border highlight fix** — ArticleList now shows MAUVE border when focused; ArticleDetail shows SURFACE0 border in preview mode
- **Scrollbar visible in preview** — ArticleDetail scrollbar now renders in both preview and full-detail modes
- **Scrollbar text overlap fix** — Sidebar, article list, and article detail now reserve 1 col for the scrollbar so text no longer renders behind it
- **Scroll Trait** — Introduced `ListScroll` struct bundling `cursor + ListState`; replaced `saved_cat_editor_cursor` with it; added `render_stateful_widget` + scrollbar to `SavedCategoryEditor`; added scroll-with-clipping to `CategoryPicker` popup.
- **Category articles view** — When sidebar cursor rests on a category header, the article panel now shows all articles from feeds in that category, with a scrollbar when the list overflows.